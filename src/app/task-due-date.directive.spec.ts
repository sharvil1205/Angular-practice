import { TaskDueDateDirective } from './task-due-date.directive';

describe('TaskDueDateDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskDueDateDirective();
    expect(directive).toBeTruthy();
  });
});
